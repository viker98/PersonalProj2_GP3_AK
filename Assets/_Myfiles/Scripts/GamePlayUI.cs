using System;
using System.Collections;
using System.Collections.Generic;
using TMPro;
using Unity.Netcode;
using UnityEngine;
using UnityEngine.UI;

public class GamePlayUI : NetworkBehaviour
{
    /*
    public static GamePlayUI Instance { get; private set; }


    [SerializeField] TextMeshProUGUI ammoCount;
    [SerializeField] Slider _healthSlider;
    private void Awake()
    {
        Instance = this;
    }

    private void Start()
    {
        gameObject.SetActive(false);
        LobbyManager.Instance.OnGameStarted += ShowHud;
    }
    public void SetHealthSlider(int value)
    { 
        _healthSlider.value = value;       
    }
    public void SetGunAmmoCount()
    {
        if(IsOwner)
        {
            
        }
    }
    private void ShowHud(object sender, EventArgs e)
    {
        gameObject.SetActive(true);
    }
    private void HideHud()
    {
        gameObject.SetActive(false);
    }
    public void AmmoChanged()
    {
        
    }
    */
}
